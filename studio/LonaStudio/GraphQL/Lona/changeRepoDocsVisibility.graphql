mutation changeRepoDocsVisibility($repoURL: String!, public: Boolean!) {
  changeRepoDocsVisibility(repoURL: $repoURL, public: $public) {
    success
    message
    repo {
      url
      activated
      docsURL
      publicDocs
    }
  }
}
